# Docker Compose

This project introduces Docker Compose and multi-container applications.

It covers:
- Services, networks and volumes
- Environment variables
- Healthchecks and dependencies
- Docker Compose commands: `up`, `down`, `logs`, `ps`, and `exec`

All project files are located in the `docker_compose` directory.

## Author

Florian Roosebeke
